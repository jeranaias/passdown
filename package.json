{
  "name": "passdown",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-cost knowledge transfer for military billet turnover",
  "scripts": {
    "serve": "npx http-server -p 8765 -c-1",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
